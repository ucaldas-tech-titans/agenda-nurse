class Turno {
  String id;
  String name;
  DateTime startDate;
  DateTime endDate;
  Turno(
      {required this.id,
      required this.name,
      required this.startDate,
      required this.endDate});
}
